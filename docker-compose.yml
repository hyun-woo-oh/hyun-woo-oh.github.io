version: "3"
# Build the development image from this repository so Gemfile.lock stays authoritative.
services:
  jekyll:
    build: .
    image: hyunwoo-oh-website
    ports:
      - 8080:8080
    volumes:
      - .:/srv/jekyll
